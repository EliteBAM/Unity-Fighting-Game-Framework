using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class practive : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Modulo: " + 4 % 2);
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // [1, 2, 3, 4, 5,....]

}
